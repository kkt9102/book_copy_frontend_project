// components
import LoginForm from '../auth/LoginForm';
import AuthTemplate from '../auth/AuthTemplate';

const Login = () => {
  return (
    <>
      <AuthTemplate>
        <LoginForm />
      </AuthTemplate>
    </>
  );
};

export default Login;
